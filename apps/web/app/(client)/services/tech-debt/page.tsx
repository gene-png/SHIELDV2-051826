import { ServiceDetailShell } from "@/components/ServiceDetailShell";

export default function TechDebtServicePage() {
  return (
    <ServiceDetailShell
      title="Technical Debt Review"
      subtitle="Inventory + overlap analysis + consolidation roadmap"
      placeholderText="The capability list and overlap dashboard ship with §8.1 of the execution plan."
    />
  );
}
