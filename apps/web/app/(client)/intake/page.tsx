export default function IntakePage() {
  return (
    <div className="mx-auto max-w-3xl">
      <h1 className="text-2xl font-bold text-navy">Intake</h1>
      <p className="mt-2 text-sm text-n-500">
        The 6-step wizard (service selection → org details → system scope → questionnaire →
        artifacts → submit) lands with §7 of the execution plan.
      </p>
      <div className="mt-8 rounded-card border border-dashed border-n-300 bg-white p-12 text-center text-n-500">
        Placeholder. Auth + chrome work; the wizard is the next build step.
      </div>
    </div>
  );
}
