# `apps/web` — SHIELD client + admin + reviewer UI

Next.js 14 App Router (TypeScript strict), Tailwind self-hosted, shadcn/ui copied into `packages/design-system`. NextAuth bound to Keycloak via OIDC.

```bash
docker compose up web
# http://localhost:3000
```
